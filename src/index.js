import React from 'react';
import ReactDOM from 'react-dom/client';
import './themes/css/index.scss';

import App from './App';

const root = ReactDOM.createRoot(document.body);
root.render(<React.StrictMode><App /></React.StrictMode>);

